# dapper-javatests-mapkey-unwrap

Demonstrates [dapper issue 15](https://github.com/jbock-java/dapper/issues/15).
To reproduce the error, open `build.gradle` and change the `dagger:2.37` dependency to `dapper:1.0`.
